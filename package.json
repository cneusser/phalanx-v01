{
  "name": "phalanx-platform",
  "version": "0.1.0",
  "description": "Phalanx M&A Platform",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "cd client && npm ci && npm run build && cd ../server && npm ci",
    "start": "NODE_ENV=production node server/index.js",
    "check:texts": "node scripts/check-texts.js",
    "test": "for f in server/tests/*.test.js; do echo \"→ $f\"; node \"$f\" || exit 1; done",
    "check:build": "cd client && npm run build",
    "check": "npm run check:texts && npm test",
    "check:full": "npm run check && npm run check:build"
  }
}
